import { NgRoomForDirective } from './ng-room-for.directive';

describe('NgRoomForDirective', () => {
  it('should create an instance', () => {
    const directive = new NgRoomForDirective();
    expect(directive).toBeTruthy();
  });
});
